package org.example.solvers.day13;

import java.util.List;

public class Instance {
    Packet left, right;

    public Instance(Packet left, Packet right) {
        this.left = left;
        this.right = right;
    }
}
